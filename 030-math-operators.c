#include <stdio.h>

// комментарии / comments
// приоритет операций / operator precedence

int main()
{
	int value;
	
	// Calculate the value
	
	value = 1 + 5 * 10;
	// value = (1 + 5) * 10;
	
	printf("%d\n", value);

	return 0;
}
